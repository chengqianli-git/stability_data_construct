# stability_data_construct
1. 根据columns可以生成parquet格式的模拟数据
2. 支持指定总文件行数
3. 支持指定生成的总数据量大小
4. 支持指定每个文件的大小
5. 支持指定每个文件的行数
6. 支持多进程并发生成
7. 生成parquet文件，json、array、map、struct以原生的方式存储# stability_data_construct
# stability_data_construct
